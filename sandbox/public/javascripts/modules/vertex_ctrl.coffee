define [
    "jquery", 
    "underscore", 
    "backbone", 
    "models/vertex"
], ($, _, Backbone, Vertex) ->
    canvas.addEventListener
    
    # X = Backbone.Model.extend
    #     defaults: ->
    #         key: "value"
    #         
    # return X